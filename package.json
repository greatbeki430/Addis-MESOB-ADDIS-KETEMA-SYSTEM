{
  "name": "addis-mesob-system",
  "version": "1.0.0",
  "private": true,
  "description": "Addis MESOB System - Monorepo for Restaurant Management",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "echo '⚠️  Root build skipped - Railway deploys backend only' && exit 0",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
